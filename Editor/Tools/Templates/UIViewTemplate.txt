using Sinkii09.UIFramework;

namespace {NAMESPACE}
{
    // partial so the wizard can drop a generated "{VIEW_NAME}.Bindings.g.cs" alongside this file.
    public partial class {VIEW_NAME} : UIView<{VIEWMODEL_NAME}>
    {
        protected override void BindViewModel({VIEWMODEL_NAME} vm)
        {
            // Set up R3 bindings here.
            // Subscriptions that should live only during Show→Hide: .AddTo(ref _showDisposables)
        }
    }
}
